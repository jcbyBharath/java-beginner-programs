 Map<String,Long> namesCount = names
         *                           .stream()
         *                           .collect(
         *                            Collectors.groupingBy(
         *                              Function.identity()
         *                            , Collectors.counting()
         *                            ));
         * System.out.println(namesCount);